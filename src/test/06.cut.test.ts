import { cutRoute } from "./06.cut"

describe('06.cut.test.ts', () => {
  it('should read text file and split into lines', () => {
    const result = cutRoute()
    expect(result).toBe('')
  })
})

